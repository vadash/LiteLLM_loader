# ==============================================================================
# LiteLLM Router Configuration for OpenVault
# ==============================================================================
# This configuration file manages a highly resilient, load-balanced, and
# self-healing gateway that routes LLM requests across multiple backend providers.
#
# How Routing Works Here:
# 1. Clients send requests to virtual entry points (e.g., "FAST" or "SMART").
# 2. A custom handler (handler.py) intercepts the request *before* routing.
# 3. The handler rewrites the model to the primary target and injects the
#    appropriate fallback chain.
# 4. If a provider returns garbage, safety refusals, or crashes, the handler
#    cooldowns that specific deployment for 10 minutes (600s), and the router
#    seamlessly tries the next option in the fallback list.
# ==============================================================================

model_list:
# ────────────────────────────────────────────────────────────────────────────
# 2. Reusable Fallback & Load-Balancing Groups
# ────────────────────────────────────────────────────────────────────────────
# When multiple entries share the same 'model_name', LiteLLM automatically groups
# them into a load-balanced pool. If one fails, LiteLLM tries the others.
# ────────────────────────────────────────────────────────────────────────────

# --- NVIDIA Fast Pool: Mistral Large 3 675B + Step 3.7 Flash ---
- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_1
    api_key: os.environ/NVIDIA_API_KEY_1
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_2
    api_key: os.environ/NVIDIA_API_KEY_2
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_3
    api_key: os.environ/NVIDIA_API_KEY_3
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_4
    api_key: os.environ/NVIDIA_API_KEY_4
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_5
    api_key: os.environ/NVIDIA_API_KEY_5
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_6
    api_key: os.environ/NVIDIA_API_KEY_6
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_7
    api_key: os.environ/NVIDIA_API_KEY_7
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_8
    api_key: os.environ/NVIDIA_API_KEY_8
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_9
    api_key: os.environ/NVIDIA_API_KEY_9
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_10
    api_key: os.environ/NVIDIA_API_KEY_10
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_11
    api_key: os.environ/NVIDIA_API_KEY_11
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_12
    api_key: os.environ/NVIDIA_API_KEY_12
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_13
    api_key: os.environ/NVIDIA_API_KEY_13
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_14
    api_key: os.environ/NVIDIA_API_KEY_14
    drop_params: true
    rpm: 2

- model_name: nvidia/cheap
  litellm_params:
    model: mistralai/mistral-large-3-675b-instruct-2512
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_15
    api_key: os.environ/NVIDIA_API_KEY_15
    drop_params: true
    rpm: 2

# --- NVIDIA GLM-5.2 Pool ---
- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_1 # Resolves from environment variable at runtime
    api_key: os.environ/NVIDIA_API_KEY_1
    drop_params: true # Drop unsupported parameters to prevent 400 Bad Request errors
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_2
    api_key: os.environ/NVIDIA_API_KEY_2
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_3
    api_key: os.environ/NVIDIA_API_KEY_3
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_4
    api_key: os.environ/NVIDIA_API_KEY_4
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_5
    api_key: os.environ/NVIDIA_API_KEY_5
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_6
    api_key: os.environ/NVIDIA_API_KEY_6
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_7
    api_key: os.environ/NVIDIA_API_KEY_7
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_8
    api_key: os.environ/NVIDIA_API_KEY_8
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_9
    api_key: os.environ/NVIDIA_API_KEY_9
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_10
    api_key: os.environ/NVIDIA_API_KEY_10
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_11
    api_key: os.environ/NVIDIA_API_KEY_11
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_12
    api_key: os.environ/NVIDIA_API_KEY_12
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_13
    api_key: os.environ/NVIDIA_API_KEY_13
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_14
    api_key: os.environ/NVIDIA_API_KEY_14
    drop_params: true
    rpm: 2 # to spread the load

- model_name: nvidia/smart
  litellm_params:
    model: z-ai/glm-5.2
    custom_llm_provider: "openai"
    api_base: os.environ/NVIDIA_PROXY_BASE_15
    api_key: os.environ/NVIDIA_API_KEY_15
    drop_params: true
    rpm: 2 # to spread the load

# --- Google Gemma 4 Pool ---
- model_name: google/gemma4
  litellm_params:
    model: gemma-4-31b-it
    custom_llm_provider: "openai"
    api_base: os.environ/GOOGLE_PROXY_BASE_1
    api_key: os.environ/GOOGLE_API_KEY_1
    drop_params: true
    # Exclude specific parameters that cause Gemma engines to error out:
    additional_drop_params: [ "frequency_penalty", "presence_penalty", "do_sample", "chat_template_kwargs" ]
    rpm: 5 # to spread the load

- model_name: google/gemma4
  litellm_params:
    model: gemma-4-31b-it
    custom_llm_provider: "openai"
    api_base: os.environ/GOOGLE_PROXY_BASE_2
    api_key: os.environ/GOOGLE_API_KEY_2
    drop_params: true
    additional_drop_params: [ "frequency_penalty", "presence_penalty", "do_sample", "chat_template_kwargs" ]
    rpm: 5 # to spread the load

- model_name: google/gemma4
  litellm_params:
    model: gemma-4-31b-it
    custom_llm_provider: "openai"
    api_base: os.environ/GOOGLE_PROXY_BASE_3
    api_key: os.environ/GOOGLE_API_KEY_3
    drop_params: true
    additional_drop_params: [ "frequency_penalty", "presence_penalty", "do_sample", "chat_template_kwargs" ]
    rpm: 5 # to spread the load

# --- LongCat Pool ---
- model_name: longcat
  litellm_params:
    model: LongCat-2.0
    custom_llm_provider: "anthropic"
    api_base: os.environ/LONGCAT_PROXY_BASE_1
    api_key: os.environ/LONGCAT_API_KEY
    rpm: 10
    drop_params: true

# --- ZAI GLM Pools ---
- model_name: zai/glm52
  litellm_params:
    model: glm-5.2
    custom_llm_provider: "anthropic"
    api_base: os.environ/ZAI_PROXY_BASE_1
    api_key: os.environ/ZAI_API_KEY
    rpm: 10
    supports_reasoning: true # Tells LiteLLM to preserve reasoning/thinking tokens
    # Tell the parser to permit these reasoning controls to reach the model
    supported_openai_params: [ "thinking", "clear_thinking" ]
    additional_drop_params: [ "frequency_penalty", "presence_penalty" ]

- model_name: zai/glm51
  litellm_params:
    model: glm-5.1
    custom_llm_provider: "anthropic"
    api_base: os.environ/ZAI_PROXY_BASE_1
    api_key: os.environ/ZAI_API_KEY
    rpm: 10
    supports_reasoning: true # Tells LiteLLM to preserve reasoning/thinking tokens
    # Tell the parser to permit these reasoning controls to reach the model
    supported_openai_params: [ "thinking", "clear_thinking" ]
    additional_drop_params: [ "frequency_penalty", "presence_penalty" ]

- model_name: zai/glm47
  litellm_params:
    model: glm-4.7
    custom_llm_provider: "anthropic"
    api_base: os.environ/ZAI_PROXY_BASE_1
    api_key: os.environ/ZAI_API_KEY
    rpm: 10
    supports_reasoning: true # Tells LiteLLM to preserve reasoning/thinking tokens
    # Tell the parser to permit these reasoning controls to reach the model
    supported_openai_params: [ "thinking", "clear_thinking" ]
    additional_drop_params: [ "frequency_penalty", "presence_penalty" ]

# old shit but redirects internally to glm47. Saves plan quota
- model_name: zai/glm-4.5-air
  litellm_params:
    model: glm-4.5-air
    custom_llm_provider: "anthropic"
    api_base: os.environ/ZAI_PROXY_BASE_1
    api_key: os.environ/ZAI_API_KEY
    rpm: 10
    supports_reasoning: true # Tells LiteLLM to preserve reasoning/thinking tokens
    # Tell the parser to permit these reasoning controls to reach the model
    supported_openai_params: [ "thinking", "clear_thinking" ]
    additional_drop_params: [ "frequency_penalty", "presence_penalty" ]

# ────────────────────────────────────────────────────────────────────────────
# Router Settings
# ────────────────────────────────────────────────────────────────────────────
router_settings:
  # Strategy: Evaluates past response times and routes traffic to the fastest healthy model.
  routing_strategy: "latency-based-routing"
  routing_strategy_args:
    lowest_latency_buffer: 0.3 # If models are within 0.3s of each other, randomly load-balance

  default_litellm_params:
    # Keep common client-side sampling knobs that OpenAI-compatible backends reject
    # out of every provider request. Per-deployment lists below are a subset of this.
    additional_drop_params: [ "frequency_penalty", "presence_penalty", "do_sample", "chat_template_kwargs" ]
    extra_headers:
      # Some public APIs block standard python-requests headers. We mock a browser UA to prevent blocks.
      User-Agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/146.0.0.0 Safari/537.36"

  # Fallback Definitions:
  # LiteLLM processes fallback chains in a flat manner. It does not recurse.
  # If a model fails, it steps through this explicit list index-by-index.
  fallbacks:
  - nvidia/cheap: [ "google/gemma4", "zai/glm-4.5-air", "longcat" ]
  - nvidia/smart: [ "zai/glm52", "longcat" ]
  - google/gemma4: []
  - longcat: []
  - zai/glm52: []
  - zai/glm51: []
  - zai/glm47: []
  - zai/glm-4.5-air: []
  # Content policy fallbacks are matched if a provider explicitly rejects a prompt due to their moderation policy.
  content_policy_fallbacks:
  #- FAST: [ "nvidia/cheap", "google/gemma4", "longcat", "zai/glm-4.5-air", "nvidia/smart", "zai/glm47" ]
  #- SMART: [ "nvidia/smart", "zai/glm52", "longcat" ]
  - nvidia/cheap: []
  - nvidia/smart: []
  - google/gemma4: []
  - longcat: []
  - zai/glm52: []
  - zai/glm51: []
  - zai/glm47: []
  - zai/glm-4.5-air: []

  timeout: 180 # Timeout calls to a provider after 180 seconds
  num_retries: 1 # Allow 1 retry to wait out brief rate limits before falling back
  enable_pre_call_checks: false # Set to false to avoid making ping calls prior to every request (saves latency)
  allowed_fails: 1 # Put a deployment on cooldown after 1 failed/garbage response
  cooldown_time: 600 # Keep failing providers dead for 10 minutes (600s)

  # Per-Error Cooldown Settings:
  # Defines how many times specific errors can occur on a deployment before it is temporarily disabled.
  allowed_fails_policy:
    # 500 Errors: Cooldown immediately. This catches synthetic 500s from your custom garbage handler.
    InternalServerErrorAllowedFails: 1

    # 400 Errors: Cooldown immediately. This catches safety or strict context limit rejects.
    BadRequestErrorAllowedFails: 1

    # 502 Errors: Cooldown immediately. Catches connection drops or bad gateways.
    BadGatewayErrorAllowedFails: 1

    # 429 Errors: Allow 2 attempts before cooldown to give brief rate-limits a chance to clear.
    RateLimitErrorAllowedFails: 2

    # Timeout: Allow 2 timeout occurrences before temporarily disabling the endpoint.
    TimeoutErrorAllowedFails: 2

    # Content Moderation: Cooldown immediately to switch to a less restrictive model in the chain.
    ContentPolicyViolationErrorAllowedFails: 1

# ────────────────────────────────────────────────────────────────────────────
# General Settings
# ────────────────────────────────────────────────────────────────────────────
general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY # The admin key used to manage the LiteLLM server
  background_health_checks: false # Avoid background ping spam; let real requests discover failures

# ────────────────────────────────────────────────────────────────────────────
# LiteLLM Core Behavior Settings
# ────────────────────────────────────────────────────────────────────────────
litellm_settings:
  callbacks: [ "handler.custom_handler" ] # Connects the custom garbage response handler (handler.py)
  set_verbose: false # Keep console logs clean (mute LiteLLM verbose debugging) # Keep console logs clean (mute LiteLLM verbose debugging)

  # MUST BE FALSE: If set to True globally, LiteLLM would purge 'thinking' parameters before the router logic can run.
  drop_params: false

  modify_params: true

  # MUST BE TRUE: Directs LiteLLM to translate reasoning blocks (like Anthropic thinking tags) into standard OpenAI reasoning fields.
  include_reasoning: true

  # Forces LiteLLM to pass non-standard parameters down to the provider API instead of stripping them.
  force_extra_body: true
