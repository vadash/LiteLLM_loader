# LiteLLM Proxy Agent Guide

This repository runs a LiteLLM proxy that load-balances equivalent models across multiple proxy base URLs and falls back across provider model groups.

## Progressive disclosure

- [Routing and callback lifecycle](agent_docs/routing.md) — read before changing `src/config.yaml`, response validation, retries, or deployment cooldown behavior.

